# Database (required)
DB_NAME=django_db
DB_USER=django_user
DB_PASSWORD=django_password
DB_HOST=db
DB_PORT=5432

# Django (required for Docker via entrypoint.sh)
SECRET_KEY=change-me-to-a-long-random-string

# Optional
DEBUG=1
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
